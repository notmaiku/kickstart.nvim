-- ~/.config/nvim/lua/plugins/vim-vue.lua
return {
  {
    'posva/vim-vue',
    lazy = false, -- Load immediately (not lazy-loaded)
    config = function()
      -- Optional: Add custom configurations for vim-vue
    end,
  },
}
